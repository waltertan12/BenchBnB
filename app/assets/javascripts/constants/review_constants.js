(function (root) {
  if (typeof root.ReviewConstants === "undefined") {
    root.ReviewConstants = {}
  }

  root.ReviewConstants = {
    REVIEWS_RECEIVED: "REVIEWS_RECEIVED",
    REVIEW_RECEIVED: "REVIEW_RECEIVED",
    REVIEW_CREATED: "REVIEW_CREATED"
  };

})(this);