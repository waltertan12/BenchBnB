(function (root) {
  if (typeof root.FilterParamsConstants === "undefined") {
    root.FilterParamsConstants = {}
  }

  root.FilterParamsConstants = {
    FILTER_UPDATED: "FILTER_UPDATED"
  };

})(this);