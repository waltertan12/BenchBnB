(function (root) {
  if (typeof root.BenchConstants === "undefined") {
    root.BenchConstants = {}
  }

  root.BenchConstants = {
    BENCHES_RECEIVED: "BENCHES_RECEIVED",
    BENCH_CREATED: "BENCH_CREATED"
  };

})(this);